# ============================================================
# 简历优化助手 · 依赖清单
# ============================================================
# 安装方式：
#   pip install -r requirements_resume.txt
#
# 国内用户建议使用清华镜像加速：
#   pip install -r requirements_resume.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# ============================================================

# ── Web 框架 ──
# >=1.37：免费额度功能依赖 st.context.headers 识别用户 IP
streamlit>=1.37.0

# ── LLM 调用（OpenAI 兼容 SDK）──
openai>=1.0.0

# ── 环境变量管理 ──
python-dotenv>=1.0.0

# ── RAG / 向量数据库 ──
langchain>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
chromadb>=0.5.0

# ── 文本向量化（TF-IDF）──
scikit-learn>=1.3.0

# ── BGE 语义向量（可选，国内节点用镜像）──
# 如需使用 BGE 语义向量模型，取消下面注释：
# sentence-transformers>=2.7.0

# ── 数据处理 ──
pandas>=1.5.0
numpy>=1.24.0

# ── 文件解析（可选）──
# PDF 解析（二选一或都装）：
# PyPDF2>=3.0.0
# pdfplumber>=0.10.0
# DOCX 读写：
# python-docx>=0.8.0
